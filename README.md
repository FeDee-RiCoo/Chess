# Chess
    board.py --> si occupa della creazione di una griglia 8x8
    pieces.py --> disegno e rappresentazione di ogni pezzo tramite turtle
    game.py --> logiche di gioco
    main.py --> inizializza la screen collega gli eventi del mause e avvia il loop